{
  "name": "worksense",
  "description": "build scripts for heroku",
  "engines": {
    "node": ">= 14"
  },
  "scripts":{
    "build": "npm install --prefix client && npm run build --prefix client",
    "deploy": "cp -a client/build. /public",
    "heroku-postbuild": "npm run build && npm tun deploy"
  }
}
